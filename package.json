{
  "name": "pico4u-vr-sleep-workaround",
  "version": "0.2.0",
  "description": "Pico 4 Ultra Sleep Workaround",
  "author": "ryium",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=24 <27"
  },
  "scripts": {
    "postinstall": "pnpm -C frontend install --frozen-lockfile",
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "version": "node scripts/sync-version.mjs && git add frontend/package.json Cargo.toml tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
